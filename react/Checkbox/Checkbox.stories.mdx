<!-- Checkbox.stories.mdx -->

import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { Checkbox } from './Checkbox';

<Meta title="React/Checkbox" component={Checkbox} />

export const Template = (args) => <Checkbox {...args} />;

# Checkbox

With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.

<Canvas>
  <Story 
    name="Unchecked"
    args={{ 
      label: 'Unchecked',
    }}>
    {Template.bind({})}
   </Story>
  <Story 
    name="Checked"
    args={{ 
      label: 'Checked', 
      isChecked: true,
    }}>
    {Template.bind({})}
   </Story>
  <Story 
    name="Disabled"
    args={{
      label: 'Disabled', 
      isChecked: true, 
      isDisabled: true,
    }}>
    {Template.bind({})}
   </Story>
</Canvas>